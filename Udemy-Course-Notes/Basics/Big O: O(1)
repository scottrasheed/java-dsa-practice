public class ConstantTimeExample {

    // Method that demonstrates O(1) time complexity
    public static int getElementAtIndex(int[] arr, int index) {
        // Accessing an element in an array by index is O(1)
        return arr[index];
    }

    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5}; // Array with 5 elements
        
        // Get the element at index 2 (which is 3)
        int element = getElementAtIndex(arr, 2);
        
        System.out.println("Element at index 2: " + element);
        
        // Explanation:
        // This is an O(1) operation because accessing any element in the array by index
        // takes the same amount of time, regardless of the array's size.
    }
}
